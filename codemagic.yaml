workflows:
  ryde-unlock:
    name: Ryde BLE Unlock
    instance_type: mac_mini_m2
    max_build_duration: 60
    scripts:
      - name: Build
        script: |
          echo "Build setup"
          mkdir -p build/output
    artifacts:
      - '**/*.ipa'

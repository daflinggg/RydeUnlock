workflows:
  ryde-unlock:
    name: Ryde BLE Unlock
    instance_type: mac_mini_m2
    max_build_duration: 60
    triggering:
      push:
        branches:
          - main
    scripts:
      - name: Build
        script: |
          echo "Simple build setup"
          mkdir -p build/output
    artifacts:
      - '**/*.ipa'
